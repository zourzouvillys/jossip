package io.rtcore.sip.bind;

import java.io.IOException;
import java.nio.charset.StandardCharsets;

import org.jeasy.states.api.FiniteStateMachineException;
import org.junit.jupiter.api.Test;

import com.google.common.io.Resources;

class SipMessageBinderTest {

  @Test
  void test() throws IOException, FiniteStateMachineException {
    //String query = Resources.asCharSource(Resources.getResource("test.query"), StandardCharsets.UTF_8).read();
    //Query q = new SipMessageBinder().bind(query);
    //SipMessageContext.fromUtf8("INVITE");
  }

}
